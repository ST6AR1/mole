{
  "name": "mole-windows-detect",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Windows support: a cross-platform port of bin/smart-launch.sh's project-type detection, the launcher/port-polling/stop runtime layer, and an Electron shell on top of both. See README.md.",
  "main": "dist/electron/main.js",
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "build:electron": "node scripts/build-electron.mjs",
    "start": "npm run build:electron && electron ."
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "electron": "^44.4.0",
    "typescript": "^5.7.0"
  }
}
